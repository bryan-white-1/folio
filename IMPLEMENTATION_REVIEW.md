갱신: 2026-09-09 12:18:35 KST (UTC+09:00)

Windows Markdown Editor 구현 검토안

요청 범위: Windows 설치형 앱, 좌측 제목 기반 Context Tree, 우측 원문·렌더링 모드 전환, 두 모드에서 본문 편집. 신규 개발 및 Windows 개발 언어 학습을 전제로 검토했다. 현재 작업 폴더에는 기존 구현 파일이 없다. 아래 내용은 구현 전 설계 제안이며 성능·호환성 검증 결과가 아니다.

권장 구성은 **C# + WinUI 3 + WebView2 + TypeScript**다. C#은 Windows 창·파일·설정을, TypeScript는 Markdown 편집·변환을 담당한다. 렌더링 편집에는 Milkdown, 원문 편집에는 CodeMirror 6를 우선 검증한다. Microsoft는 신규 네이티브 Windows 앱에 WinUI 3를 권장하며, WebView2를 통해 앱 내부에 웹 편집기를 삽입할 수 있다. [Windows 개발 경로](https://learn.microsoft.com/en-us/windows/apps/get-started/), [WinUI 3 WebView2](https://learn.microsoft.com/en-us/windows/apps/develop/ui/controls/webview2)

| 기술안 | 사용 언어·UI | 적합한 조건 | 검토 의견 |
|---|---|---|---|
| WinUI 3 + WebView2 | C#, XAML, TypeScript, HTML/CSS | Windows 전용 신규 앱·네이티브 개발 학습 | 우선 권장. 네이티브·웹 경계의 상태와 포커스 관리 필요 |
| WPF + WebView2 | C#, XAML, TypeScript, HTML/CSS | 기존 WPF 경험·자산 활용 | 유효한 대안. 기존 경험이 있다면 우선순위를 높일 수 있음 |
| Tauri 2 | Rust, TypeScript, HTML/CSS | 향후 여러 OS 지원·Rust 학습 | Windows에서 WebView2 사용. Rust 및 빌드 도구 학습 필요 |
| Electron | TypeScript/JavaScript, HTML/CSS | 웹 개발 경험 활용·여러 OS 지원 | Chromium·Node.js 동봉. 배포 크기·메모리 예산 검증 필요 |

위 비교의 적합성은 요구사항에 대한 설계 판단이다. WPF의 WebView2 지원, Tauri의 Windows 개발 요건, Electron의 런타임 구성은 각 공식 문서에 근거한다. [WPF WebView2](https://learn.microsoft.com/en-us/microsoft-edge/webview2/get-started/wpf), [Tauri 사전 요건](https://v2.tauri.app/start/prerequisites/), [Electron 소개](https://www.electronjs.org/docs/latest/)

개발 언어는 C#과 TypeScript 두 가지를 중심으로 익히면 된다. XAML은 네이티브 화면 선언, HTML/CSS는 편집 영역 구조·스타일에 사용한다. 이 구성에서 C++은 필수가 아니다. 완전한 C# 전용 구현도 가능하지만 Markdown 시각 편집·변환 엔진의 직접 구현 또는 전용 상용 컨트롤 검토 부담이 커진다.

| 담당 | 기술 | 구현 범위 |
|---|---|---|
| Windows 앱 | C#·WinUI 3·XAML | 창, 메뉴, Context Tree, 파일 대화상자, 최근 파일, 설정 |
| 원문 편집 | TypeScript·CodeMirror 6 | 구문 강조, 찾기·바꾸기, 줄 번호, 선택·커서 |
| 렌더링 편집 | TypeScript·Milkdown/ProseMirror | 제목·문단·목록·인용·표 등 구조화 편집 |
| 문서 조정 | TypeScript DocumentController | 원문 버퍼, revision, 문법 트리, 편집 패치, 이력, 위치 매핑 |
| 로컬 저장 | C# FileService | 열기·저장, 이미지 파일, 복구본, 외부 변경 감지 |
| 앱 연결 | WebView2 메시지 | 문서 로드·저장 요청, 목차 전달, 제목 이동·편집 명령 |

Milkdown은 Markdown을 Remark AST와 ProseMirror 문서 모델 사이에서 변환하는 구조다. CodeMirror는 문서·선택 변경을 트랜잭션으로 처리한다. 두 라이브러리를 함께 사용한다고 앱 전체의 원문 보존·Undo 동기화가 자동 해결되지는 않는다. [Milkdown 구조](https://milkdown.dev/docs/guide/architecture-overview), [CodeMirror 상태·트랜잭션](https://codemirror.net/docs/guide/)

화면은 좌측 240~300px 목차와 우측 가변 폭 본문을 기본값으로 제안한다. 경계 드래그, 목차 접기, 밝은·어두운 테마, 적절한 본문 행간·최대 폭을 제공한다. 상단에는 파일명·저장 상태·원문/렌더링 전환, 하단에는 현재 제목 경로·커서 위치를 둔다. 시각 편집 도구는 본문 선택 시 표시해 문서 영역을 확보한다.

```text
┌─────────────────────────────────────────────────────┐
│ 문서.md · 저장됨             [렌더링] [원문]   저장   │
├────────────────┬────────────────────────────────────┤
│ 목차           │ # 프로젝트 개요                    │
│ ▾ 프로젝트 개요│                                    │
│   ▾ 기능       │ 편집 가능한 렌더링 본문             │
│      파일 관리 │ 또는 Markdown 원문                 │
│      편집      │                                    │
│   ▸ 구현       │                                    │
├────────────────┴────────────────────────────────────┤
│ 프로젝트 개요 > 기능                  변경·커서 상태 │
└─────────────────────────────────────────────────────┘
```

| ID | 1차 기능 SPEC | 동작·판정 기준 |
|---|---|---|
| F01 | 문서 파일 | 새 문서, .md 열기, 저장, 다른 이름 저장; 저장 성공 응답 후 저장 상태 갱신 |
| F02 | Context Tree | 본문 문법 분석 결과의 H1~H6으로 계층 생성; 펼침·접기 지원 |
| F03 | 제목 이동 | 트리 선택 시 해당 제목으로 이동; 본문 커서 이동 시 현재 제목 표시 |
| F04 | 원문 편집 | Markdown 원문 직접 수정, 구문 강조, 검색·치환 |
| F05 | 렌더링 편집 | 지원 문법 범위에서 제목·문단·강조·링크·이미지·목록·체크리스트·인용·코드·표 편집 |
| F06 | 모드 전환 | 원문↔렌더링 전환 시 최신 내용 반영; 대응 가능한 커서·선택·스크롤 위치 복원 |
| F07 | 편집 이력 | 원문·렌더링·트리 명령의 변경을 문서 단위 Undo/Redo로 관리; 전환 자체는 변경 이력에 추가하지 않음 |
| F08 | 복구·충돌 | 변경 상태 표시, 복구본 저장, 종료 시 미저장 처리, 외부 수정 충돌 선택 |
| F09 | Windows UI | 창 크기·테마·목차 폭 보관; 키보드 탐색·고배율 표시 지원 |

‘양쪽 수정’은 원문과 렌더링 두 모드의 본문 편집으로 해석한다. 좌측 트리에서도 수정하려는 경우, 제목 이름 변경은 후속 기능으로 쉽게 연결할 수 있다. 제목 드래그 이동·삭제는 하위 본문 전체를 다루므로 별도 구조 편집 기능으로 구분한다.

목차는 정규식으로 `#`를 찾는 방식 대신 Markdown 파서의 heading 노드를 사용한다. 코드 블록 안의 `#`를 제외하고 Setext 제목도 인식한다. 깊이가 건너뛰면 가장 가까운 상위 깊이 제목 아래에 연결한다. 중복 제목은 텍스트 대신 세션 내부 ID와 위치로 구분한다. 초기 범위는 문서 최상위 흐름의 제목으로 정하고, 인용·목록 내부 제목은 목차에서 제외한다. [mdast heading 정의](https://github.com/syntax-tree/mdast)

목차 노드의 제안 필드는 `id`, `depth`, `label`, `headingRange`, `sectionRange`, `parentId`다. 섹션 범위는 해당 제목 시작부터 다음 동일·상위 깊이 제목 직전 또는 문서 끝까지다. 제목 이동을 추가할 때는 하위 섹션을 함께 이동하고, 자기 하위로 이동하는 순환을 금지하며, 중첩 변경에 따른 H1~H6 깊이 초과를 검사한다.

**편집 상태의 기준은 하나의 Markdown 원문 버퍼로 둔다.** AST·렌더링 문서·목차는 해당 revision에서 파생된 상태다. C#에 전달한 저장용 사본을 별도의 편집 기준으로 사용하지 않는다.

1. 원문 입력: 편집 변경을 원문 버퍼에 적용 → revision 증가 → 파싱 → 목차 갱신 → 렌더링 모델은 전환 시 갱신.
2. 렌더링 입력: ProseMirror 변경을 Markdown 패치로 변환 → 원문 버퍼에 적용 → revision 증가 → 목차·위치 매핑 갱신.
3. 모드 전환: 한글 조합 완료·미반영 변경 확정 → 최신 revision 표시 → 문서 노드/원문 위치로 커서 복원.
4. 트리 명령: 현재 revision의 제목 ID로 위치 확인 → 공통 편집 경로에 명령 전달 → 결과 목차 재전달.
5. 저장: `documentId`, `revision`, 원문을 C#에 전달 → 파일 기록 완료 응답 → 저장한 revision 이후 변경이 없을 때만 ‘저장됨’ 표시.

원문 문자 위치와 렌더링 문서 위치는 서로 다르므로 변환표가 필요하다. 처음에는 제목·블록 단위 대응을 구현하고, 정확한 선택 영역 대응을 확대한다. 문법 변경으로 대응 지점이 사라지면 가장 가까운 유효 위치에 커서를 둔다. 렌더링 모델을 매 타이핑마다 전체 교체하면 한글 조합·선택·Undo가 끊길 수 있으므로 활성 편집기 내부 트랜잭션을 유지해야 한다.

앱 수준의 통합 이력은 별도 구현 대상이다. 원문 패치와 역패치, 변경 전후 선택 위치를 저장하고 연속 입력을 묶는다. 각 편집기의 Undo와 앱 이력이 중복 실행되지 않도록 키보드·메뉴 명령을 연결한다. 두 모드의 독립 Undo만 유지하는 방식은 모드 전환 이후 이전 변경을 일관되게 취소하지 못할 수 있다.

**내용 보존과 원문 표현 보존은 구분해야 한다.** 예를 들어 `* 항목`과 `- 항목`, `__강조__`와 `**강조**`는 같은 의미라도 원문이 다르다. Markdown 전체를 파싱한 뒤 다시 직렬화하면 공백·빈 줄·목록 기호·링크 표현이 정규화될 수 있다. 이는 변환 구조에서 예상되는 구현 위험이며, Milkdown의 바이트 단위 보존 보장을 확인한 것이 아니다.

| 보존 정책 | 권장 동작 | 구현 난도 |
|---|---|---|
| 편집 없는 열기·전환·저장 | 원본 내용과 BOM·개행을 그대로 보존; 단순 전환으로 dirty 상태를 만들지 않음 | 기본 요건 |
| 지원 문법의 시각 편집 | 내용·문서 구조 보존; 수정 구간의 Markdown 표기는 정규화 가능 | MVP 권장 |
| 비수정 영역 원문 보존 | source range와 원문 조각을 유지하고 필요한 범위만 교체 | 별도 패치 계층 필요 |
| 알 수 없는 확장 문법 | 원문 유지; 안전한 원문 블록으로 표시·수정. 보호가 어려우면 해당 문서 시각 편집을 제한하고 이유 표시 | 사용자 정의 노드·탐지 필요 |
| 임의 Markdown의 완전한 왕복 보존 | 공백·주석·참조 정의까지 보존하는 변환 계층과 광범위한 검증 | 초기 범위로 권장하지 않음 |

최소 텍스트 diff만 적용해도 전체 serializer가 다른 부분을 정규화했다면 비수정 영역 보존은 해결되지 않는다. 정확한 보존에는 원문 범위·노드 연결 및 블록 간 참조/목록 경계 처리까지 필요하다. 지원 문법은 CommonMark 기반에 GFM 표·취소선·체크리스트를 명시적으로 추가하고, 실제 지원 여부는 채택한 버전·플러그인 조합으로 검증한다.

초기 앱은 로컬 파일 기반으로 설계한다. 웹 편집기 자산을 앱에 포함하면 본문 편집을 위한 서버·로그인·인터넷 연결은 필요 없다. 설정은 JSON으로 충분하며 DB·AI는 초기 요구 기능에 포함하지 않는다. 이미지는 문서 인접 assets 폴더와 상대 경로를 기본안으로 두고, ‘다른 이름 저장’ 시 이미지 경로 유지·복사 정책을 적용한다.

파일 기록은 같은 디렉터리 임시 파일 기록 후 교체하는 방식으로 손상 가능성을 줄이고 실패 시 기존 파일·복구본을 유지한다. 외부 변경 감지는 디스크 상태를 다시 확인해 자체 저장 이벤트와 구분한다. UTF-8을 기본으로 BOM·CRLF/LF를 기록하고, 해석이 불명확한 기존 인코딩은 조용히 변환하지 않고 사용자가 선택하도록 한다.

WebView2에는 앱에 포함한 편집기만 로드한다. Markdown의 HTML·링크는 신뢰하지 않는 입력으로 처리하며 실행 가능한 HTML을 기본 차단하고 C# 메시지의 출처·명령·인자를 검증한다. 파일 접근은 열린 문서와 사용자가 지정한 파일 범위로 한정한다. 이는 로컬 문서 내용이 파일 API를 호출하는 경로를 차단하기 위한 설계다. [WebView2 보안 지침 포함 문서](https://learn.microsoft.com/en-us/windows/apps/develop/ui/controls/webview2)

| 단계 | 작업 | 완료 조건 |
|---|---|---|
| 1. 편집기 기술 검증 | WebView2 내부에서 Milkdown·CodeMirror 전환, 한글 입력, 표·이미지·Undo 검증 | 지원 문법과 원문 보존 정책을 실제 표본으로 확정 |
| 2. Windows MVP | WinUI 창·목차·열기·저장·두 모드 편집 연결 | 파일 재열기 후 내용 일치, 목차 이동·갱신 정상 |
| 3. 안정화 | 통합 이력, 복구, 외부 변경, 인코딩, 접근성·성능 | 입력 손실·저장 충돌·전환 오류 검증 통과 |
| 4. 배포 | MSIX, 실행 환경 의존성, 설치·업데이트·파일 연결 | 개발 도구 없는 별도 Windows PC에서 설치·편집·저장 성공 |
| 5. 선택 확장 | 트리 제목 수정·섹션 이동, 다중 탭, Mermaid·수식 | 기본 편집·보존 회귀 검증 통과 |

학습 순서는 C# 파일 입출력·비동기 처리 → WinUI XAML·바인딩·명령 → TypeScript 이벤트·상태 → Markdown 파싱·편집기 트랜잭션 → WebView2 메시지 → 설치 패키지 순서를 제안한다. 개발 도구는 Visual Studio의 WinUI/.NET 구성과 웹 자산 빌드를 위한 Node.js 도구를 사용하며 SDK·패키지 버전은 프로젝트 생성 시 호환되는 안정 버전으로 고정한다.

배포는 MSIX를 우선 검토하고 직접 배포 시 패키지 서명·업데이트 경로를 결정한다. .NET·Windows App SDK의 배포 방식과 WebView2 Runtime 의존성을 각각 확인해야 한다. WebView2 Runtime이 없는 PC에는 설치 경로가 필요하고, 완전 오프라인 설치는 런타임 설치 파일 포함을 검토한다. MSIX 생성만으로 모든 의존성이 자동 해결된다고 가정하지 않는다. [Windows 패키징](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/packaging/), [WebView2 배포](https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/distribution)

우선 검증할 표본은 한글 조합 중 전환·저장, 중복 제목, H1→H3 깊이 건너뛰기, 코드 안의 `#`, Setext 제목, 중첩 목록, 표의 `|` 이스케이프, 참조 링크, HTML·front matter, BOM·CRLF, 이모지, 긴 문서다. 100KB·1MB·10MB 표본에서 입력 지연·전환 시간·메모리를 측정한 후 지원 규모를 결정한다. 큰 문서에서는 원문 편집과 시각 편집의 지원 범위를 다르게 둘 수 있으며, 성능 수치는 측정 전에 약속하지 않는다.

최초 착수 단위는 **작은 WinUI 창 안에서 한글 Markdown을 원문↔시각 편집하고 저장·재열기하는 기술 검증**이다. 이 단계에서 편집 엔진의 적합성을 판단한 다음 목차와 Windows 기능을 확장하면 핵심 구현 위험을 먼저 확인할 수 있다.
