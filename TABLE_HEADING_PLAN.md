갱신: 2026-09-14 09:24:00 KST (UTC+09:00)

Folio 표 편집·제목 H1–H6 개선 검토안

상태: Folio 0.1.6에 1차 구현 반영. 아래는 초기 검토 기록이며 현재 계약은 `SPEC.md` E09–E15, 검증 결과는 `TEST_RESULTS.md` 기준. 구현 범위 조정: 원문 표 삽입은 최상위 문단/제목/빈 줄, 첫 셀 Shift+Tab은 제목 선택기로 이동. 행·열 이동/복제·너비 입력/복원·TSV 붙여넣기는 2차로 유지.

권장: 1차에 **본문·H1–H6 선택기 + 표 상황별 도구 + 행·열 추가/삭제 + 열 정렬 + 선택·내용 지우기 + 크기 지정 삽입**을 구현한다. 2차에 행·열 이동/복제·너비 입력/초기화·Excel 범위 붙여넣기를 추가한다. 기존 Markdown 저장·열 너비 주석·공통 Undo/Redo를 유지한다.

**현재 확인 결과**

| 항목 | 실제 구현·개선점 |
|---|---|
| 제목 UI | `web/index.html:16`의 H₂ 단일 버튼. `web/src/main.ts:188`은 렌더링 H2 고정, `:178`은 원문 선택 위치에 `# ` 삽입. 모드별 레벨 불일치·문단 중간 삽입 가능 |
| 제목 기반 기능 | Milkdown에 H1–H6 변환·Ctrl+Alt+1–6 키맵 포함. 목차 분석도 제목 깊이 지원. 제목 엔진 확장보다 UI 노출·원문 명령 일치가 핵심. H4–H6는 현재 같은 15px 스타일 |
| 표 생성 | 렌더링은 머리행 포함 3행×2열, 원문은 2행×2열 템플릿. 크기 선택·모드 간 생성 규격 통일 필요 |
| 기존 표 편집 | 셀 텍스트·인라인 서식·열 경계 드래그 지원. 행·열 추가/삭제·정렬·표 삭제 전용 메뉴 없음 |
| 기본 키보드 | 라이브러리에 Tab/Shift+Tab 셀 이동·Enter/Ctrl+Enter 표 밖 문단 삽입 포함. 마지막 셀 Tab은 자동 행 추가 명령이 아님 |
| 저장 구조 | 정렬은 머리행 기준 열 속성, 너비는 머리행 셀에서 추출한 `folio:table:v1` 주석. 표 구조 변경 시 내용·정렬·너비를 함께 처리해야 함 |
| 검증 공백 | 기존 UI 테스트는 삽입·내용 직렬화·너비 왕복 중심. 이름에 heading이 포함된 테스트도 H1–H6 변환은 실제 검사하지 않음 |

근거 파일: [툴바](web/index.html), [명령·동기화](web/src/main.ts), [목차·이력](web/src/document.ts), [너비 스키마](web/src/layout-schema.ts), [메타데이터](web/src/layout-metadata.ts), [편집 테스트](web/tests/ui/editor.spec.ts), [너비 테스트](web/tests/ui/layout.spec.ts).

**1차 기능 SPEC — 제목**

| ID | 제안 계약 |
|---|---|
| H01 선택기 | H₂ 버튼을 `본문 ▾` 선택기로 교체. 본문·제목 1(H1)–제목 6(H6) 7개 항목, 현재 서식 표시·선택 체크·크기 미리보기·단축키 안내. 여러 레벨 선택 시 `혼합` 표시 |
| H02 적용 범위 | 커서가 있는 문단 또는 선택한 문단 전체에 명시한 레벨 적용. 기존 제목 레벨 교체, 본문 선택 시 제목 해제. 동일 레벨 재선택은 변경 없음. 다른 하위 제목 레벨을 연쇄 변경하지 않음 |
| H03 원문 일치 | 렌더링·원문에서 같은 레벨 적용. 원문 AST 위치로 문단/제목 경계를 찾아 해당 범위만 패치; 문자열 앞에 무조건 `#`를 덧붙이지 않음. Setext 밑줄·ATX 닫는 `#` 처리, 인라인 서식·링크·컨테이너 접두사 보존. 여러 물리 줄의 일반 문단은 한 논리 문단으로 처리하며 소프트 줄바꿈은 공백으로 연결 |
| H04 적용 가능성 | 일반 문단·기존 제목을 대상으로 처리. 표 셀·코드·원문 보존 노드·강제 줄바꿈 포함 블록은 1차 변환 제외, 사유 표시. 제외 블록을 포함하는 선택은 전체 명령 비활성. 목록·인용 내부는 구조를 보존해 변환 가능한 경우만 허용; 목차 추출은 기존 최상위 제목 정책 유지 |
| H05 키보드·표시 | Ctrl+Alt+1–6 제목, Ctrl+Alt+0 본문을 양쪽 모드에 연결. H4–H6는 크기·굵기·여백 차이로 구분하되 기존 본문 가독성 유지. 실제 WebView2·한글 IME에서 충돌 확인 |

**1차 기능 SPEC — 표**

| ID | 제안 계약 |
|---|---|
| T01 삽입 | 표 버튼에서 빠른 크기 그리드·행/열 숫자 입력 제공. 기본 총 3행×2열, 행 수는 머리행 포함으로 명시. 신규 생성 제안 범위 2–100행·1–50열; 기존 표는 이 범위로 축소하지 않음. 양쪽 모드에 동일한 빈 표 생성, 첫 머리셀에 커서 배치 |
| T02 행 | 선택 행 위/아래에 추가, 선택한 본문 행 삭제. 첫 머리행 앞 삽입·머리행 삭제 금지. 머리행에서 아래 추가는 첫 본문 행 생성. 마지막 본문 행 삭제는 비활성, 내용 지우기·표 삭제로 안내 |
| T03 열 | 선택 열 왼쪽/오른쪽 추가, 선택 열 삭제. 선택 범위 경계를 기준으로 삽입. 마지막 열 삭제는 비활성. 새 열은 왼쪽 정렬·자동 너비 |
| T04 정렬 | 선택 셀이 속한 열 전체에 왼쪽·가운데·오른쪽 정렬. 여러 열 선택 시 모두 적용. 머리셀 속성을 변경하고 동일 열 본문 셀에 동기화하여 저장·재열기 결과 일치 |
| T05 선택·지우기 | 셀 범위 드래그, 행/열/표 전체 선택 메뉴, 선택 범위 강조. `내용 지우기`·셀 범위 선택 상태의 Delete/Backspace는 구조·정렬·너비 유지. 셀 안 텍스트 선택의 Delete는 일반 텍스트 삭제 유지 |
| T06 표 삭제 | 더보기 메뉴의 명시적 `표 삭제`로 표와 연결된 유효 너비 메타데이터 제거. 주변 문단 유지, 인접 편집 위치로 커서 이동. 문서가 비면 빈 문단 유지. 한 번의 Undo로 복원 |
| T07 이동 | Tab/Shift+Tab 다음/이전 셀. 마지막 셀 Tab은 본문 행 하나 추가 후 첫 셀 이동; 머리행 기준 정렬·열 너비 상속. 첫 셀 Shift+Tab은 이전 포커스 대상으로 이동. 기존 Enter/Ctrl+Enter 표 밖 문단 삽입은 유지하고 안내 제공 |
| T08 구조 불변 | 머리행 1개·본문 행 최소 1개·열 최소 1개 유지. 행 추가 시 각 열의 너비 상속, 열 삭제 시 해당 너비 제거. 기존 메타데이터 계약 E05–E08의 40–2400px·자동값 0·최대 256열 유지; 256열에서는 열 추가 비활성 |

**UI·이력 공통 계약**

| ID | 제안 계약 |
|---|---|
| U01 표 도구 | 렌더링에서 셀 커서/셀 선택 시 해당 표 근처에 `행 ▾ · 열 ▾ · 정렬 · ⋯` 표시. 행/열 메뉴에 추가·삭제·선택, 더보기에 내용 지우기·표 선택·표 삭제 배치. 우클릭·Shift+F10으로 같은 명령 제공. 표 밖에서는 숨김 |
| U02 선택 유지 | 메뉴를 열 때 실제 편집 선택을 보관. 선택 범위 안 우클릭은 범위 유지, 밖 우클릭은 해당 셀로 대상 변경. 메뉴 포커스 이동으로 대상이 바뀌지 않도록 처리; 실행 직전 문서·위치 유효성 재검증 |
| U03 접근성·화면 | 기존 48px 상단 툴바 유지. 표 메뉴는 본문·열 너비 손잡이를 가리지 않도록 배치하고 스크롤·좁은 화면에서 경계 보정. 마우스 hover 외 키보드 접근·명령 이름·현재 상태·비활성 사유 제공. Esc 닫기·원래 편집 위치로 초점 복원. 밝음/어두움 연동 |
| U04 이력 | 명령 전 IME 조합 확정·미반영 변경 동기화. 구조·서식 명령 하나당 공통 Undo 한 단계, 이전/이후 타이핑과 그룹 분리. 메뉴 열기·선택 이동·취소·동일 값 적용은 text/revision/dirty 불변. 저장·복구는 기존 경로 사용 |
| U05 모드 범위 | 제목 선택기·표 삽입은 양쪽 모드 지원. 기존 표의 구조 조작 메뉴는 1차 렌더링 전용, 원문은 직접 편집 유지. 단순 모드 전환 시 원문 무변경 보존. 모드 전환·문서 교체 시 메뉴 상태 폐기 |

**2차 우선순위**

| 순서 | 기능·저장 계약 |
|---|---|
| 1 | 열 너비 px 입력·선택 열 자동 복원·전체 균등 분배. 기존 40–2400px·0=자동 주석 재사용. 균등 분배는 실행 시 현재 표 폭을 기준으로 정수 너비를 확정; 이후 화면 폭 변화로 문서 수정 없음 |
| 2 | 본문 행 위/아래 이동·복제, 열 좌/우 이동·복제. 머리행은 행 이동/복제 제외. 열 이동/복제는 머리글·내용·정렬·너비 함께 처리 |
| 3 | Excel/Sheets에서 복사한 TSV 범위 붙여넣기·선택 범위 TSV 복사. 단일 셀 대상은 오른쪽/아래 확장, 다중 셀 대상은 크기가 같을 때 교체. 크기 불일치·상한 초과는 무변경 안내. 빈 셀·후행 빈 열·따옴표·셀 내부 개행 처리 검증 후 제공 |
| 4 | 표 전체 Markdown 복사·해당 표 원문 위치로 이동. 원문 이동은 최신 AST와 실제 표의 대응 검증 후 수행; 기존 제목 단위 커서 매핑을 그대로 사용하지 않음 |

TSV 1차 도입 시 셀 내부 개행은 무조건 공백으로 바꾸거나 누락하지 않는다. 별도 줄바꿈 저장 규칙이 준비되지 않았으면 적용 전 지원 불가 사유를 표시하고 문서를 유지한다. 원문 모드에서 TSV 붙여넣기는 자동 표 변환 없이 기존 텍스트 붙여넣기를 유지한다.

**후속 검토 대상**

셀 병합/분할·복수 머리행·셀별 배경색·세로 정렬·셀 내부 여러 문단은 별도 저장 설계가 필요하다. GFM 파이프 표는 단일 머리행·인라인 셀 내용·열 정렬을 정의하며 병합 표현은 제공하지 않는다. 따라서 병합은 현재 GFM 직렬화 경로만으로 보존할 수 없다는 판단이다. HTML 표 전환은 현재 Folio의 HTML 비실행·원문 보존 정책과 함께 재설계해야 한다. [GFM 표 규격](https://github.github.com/gfm/#tables-extension-)

표 머리행 자체는 유지하되 머리행의 내용을 지우는 작업은 허용한다. GFM은 본문 0행도 허용하지만, 설치된 Milkdown 스키마는 `table_header_row table_row+`를 요구하므로 T02·T08은 현재 엔진에 맞춘 제품 계약이다.

**구현 지점·주의점**

| 지점 | 구현 방향 |
|---|---|
| `heading-commands.ts` 신규 | 0–6 공통 명령·현재 레벨 계산·원문 AST 패치. 기존 `wrapInHeadingCommand`는 0 입력 시 본문 전환을 지원하므로 재사용 |
| `table-commands.ts` 신규 | Milkdown 행/열/선택 명령과 ProseMirror 삭제 명령을 제품 계약으로 감싼다. `setAlignCommand`를 본문 셀에만 실행하면 머리행 정렬로 되돌아갈 수 있어 머리셀 기준 적용. `deleteSelectedCellsCommand`는 행/열 삭제 동작이므로 내용 지우기에 직접 사용하지 않음 |
| `table-commands.ts` 너비 보정 | 설치된 `addRowWithAlignment`는 정렬만 상속한다. 신규 행 너비 상속·열 이동/삭제의 주석 동기화는 추가 처리. 범용 행 명령은 머리행/최소 본문 행 규칙을 별도 검증 |
| `format-toolbar.ts` 신규·`index.html`·CSS | 제목 선택기·표 메뉴·삽입 크기 선택 UI, 선택 보관·초점 복원·위치 계산. 제목 레벨 표시는 목차 breadcrumb가 아니라 실제 커서 블록에서 계산 |
| `main.ts`·`document.ts` | 상태 변경 시 툴바 갱신, 공통 명령 실행 경계·한글 조합 처리. 기존 `refreshRendered()`·`switchMode()`의 버튼 일괄 활성화를 적용 가능성 재계산으로 변경. 단순 메뉴 상태 변경에 `replaceAll()` 사용 금지 |

기술 판단: 1차는 설치된 의존성으로 구현 가능하며 새 편집기·서버 도입이 필요하지 않다. 범용 표 라이브러리가 행/열 추가·삭제·셀 선택 등의 기반을 제공하지만 Folio의 GFM·너비·이력 계약은 앱에서 보완한다. [ProseMirror 표 명령](https://github.com/ProseMirror/prosemirror-tables#commands)

**구현 완료 검증 기준**

| 검증 묶음 | 합격 조건 |
|---|---|
| 제목 | 양쪽 모드의 본문↔H1–H6·H간 전환·동일 값 무변경. 문단 중간 커서·여러 문단·Setext·인라인 서식·중첩 컨테이너·변환 제외 범위 확인. 목차 깊이 갱신·한글 IME·단축키 일치 |
| 표 구조 | 머리행·첫/끝 본문 행·첫/끝 열·다중 선택에서 추가/삭제/지우기 대상 정확. 최소 구조 유지·표 전체 삭제·마지막 셀 Tab 한 행 추가 |
| 왕복·이력 | 원문↔렌더링·저장 snapshot 재로드 후 내용·정렬·너비 일치. 명령별 Undo 한 단계·Redo·이전 타이핑 분리. 메뉴 조작만으로 dirty/revision 변화 없음 |
| 주변 내용 | 복수 표·인용/목록 안 표·이미지·Mermaid·유효/잘못된 레이아웃 주석 혼합 문서에서 편집 대상 외 의미·보존 토큰 유지. 기존 serializer 공백 정규화 정책은 유지 |
| UI·성능 | 밝음/어두움·좁은 창·가로/세로 스크롤·키보드 메뉴·우클릭 선택 유지. 100×50 신규 표 생성과 후속 명령의 응답성을 측정해 제안 상한 검증. Windows 호스트에서 메뉴 초점·저장/Undo 통합 확인 |

권장 구현 순서: H01–H05 → T01–T08/U01–U05 → 왕복·호스트 검증 → 구현 완료 항목만 `SPEC.md`·`README.md`·`TEST_RESULTS.md`에 반영 → 2차 기능.
