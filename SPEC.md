갱신: 2026-09-18 15:57:16 KST (UTC+09:00)

Folio 0.1.11 구현 SPEC

밝은 테마: 제목 표시줄·상단 바·목차·편집 영역 #FFFFFF, 구분선 #E5E7EB, 선택·호버 #F3F4F6, 강조 #374151. 로고 초록색 유지. 어두운 테마 유지.

창 아이콘: 창 생성 시 Assets/Folio.ico를 AppWindow.SetIcon으로 지정. 빌드·배포에 ICO 포함, 설치 패키지 필수 파일·SHA-256 검증 대상.

모서리 반경: 네이티브 편집 프레임 좌상단 8 DIP·버튼 4 DIP·로고 10 DIP 유지. 웹 버튼/입력 4px·모드 전환/코드 6px·팝오버/대화상자/다이어그램 카드 8px. OS 창 외곽·도식 내부 노드 형상 유지.

| ID | 구현 계약 |
|---|---|
| D01 | Markdown LF 정규화 문자열·revision·저장 기준·커서가 단일 편집 상태. 파싱 결과는 revision별 캐시 |
| D02 | 목차는 최상위 흐름의 heading만 추출. 깊이 건너뛰기는 가까운 상위 제목에 연결. 코드·인용 내부 제목 제외 |
| D03 | TreeViewNode.Content를 항목 템플릿으로 표시. 목차 선택은 본문 제목으로 이동. 편집 커서의 현재 제목을 트리에 반영. 제목 우클릭 변경은 원문 패치와 공통 이력에 반영 |
| E01 | 원문: CodeMirror 6. 렌더링: Milkdown/ProseMirror. 모드 전환 전 미반영 변경·IME 조합 처리 |
| E02 | 편집 없는 전환은 serializer 결과로 원문을 덮어쓰지 않음. 실제 렌더링 변경 시 전체 Markdown 직렬화 |
| E03 | 공통 이력: 변경 전 문자열·커서 스냅샷, 동일 출처 650ms 연속 입력 그룹, 전환 시 그룹 분리, 150개·8백만 문자 예산. 큰 문서도 최소 한 단계 유지 |
| E04 | HTML·참조·front matter 포함 문서도 시각 본문 편집 허용. 해당 토큰은 비실행 원문 보존 노드로 왕복 유지하고 구문 자체는 원문에서 수정. 독립 `<br />`는 편집 가능한 빈 문단. YAML/TOML 메타데이터는 목차 제외 |
| E05 | 표 셀 경계 드래그로 열 너비 조절. 가이드·가로 스크롤, 정렬 유지. 확정값 40–2400px 정수, 미지정 열은 자동. 전체 행의 같은 열에 적용 |
| E06 | Markdown 이미지 선택/호버 시 우하단 손잡이·너비 입력·원본 복원. 40–2400px, 가로 조절·종횡비 유지·컨테이너 최대 폭 제한. 손잡이 방향키 10px, Esc/포인터 취소는 미반영. 원본 파일·상대 경로·alt·title 유지 |
| E07 | 크기 저장: 표 직전 독립 주석 `<!-- folio:table:v1 widths=160,280 -->`(0=자동), 이미지 직후 인라인 주석 `<!-- folio:image:v1 width=320 -->`. 인접 AST 객체에 결합; 표 열 수 일치·최대 256열·값 검증. 중첩 표·동일 경로 이미지별 복원. 미지원/잘못된/고립 주석은 비실행 원문 보존 |
| E08 | 크기 변경은 문서 수정·공통 Undo/Redo·저장/복구에 반영, 드래그 완료당 한 이력. 무편집 모드 전환·테마·재로딩은 dirty 불변. 일반 Markdown/GFM 문법 유지; 외부 뷰어는 주석을 무시하며 지정 크기 지원은 Folio 확장 |
| E09 | 서식 선택기: 본문·H1–H6, 현재 레벨·혼합 상태·미리보기·선택 체크. 양쪽 모드 Ctrl+Alt+0–6, 0=본문. 현재/선택 문단에 적용, 하위 제목 자동 변경 없음. 동일 레벨 재선택은 무변경. H4–H6 크기·굵기·여백 구분 |
| E10 | 원문 제목 변환은 AST 블록 범위 패치. ATX·Setext·인라인 표기·지원 가능한 목록/인용 접두사 보존, 소프트 줄바꿈은 제목 변환 시 공백. 본문 복원 시 블록 기호 escape·인접 문단 분리. 표·코드·HTML/참조 보존 구간·강제 줄바꿈 포함 선택은 전체 변환 비활성 |
| E11 | 표 삽입: 8×8 빠른 선택·숫자 입력, 기본 총 3행×2열. 머리행 포함 총 2–100행·1–50열, 첫 머리셀에 커서. 렌더링은 스키마가 허용하는 문단 위치, 원문은 최상위 일반 문단/제목/빈 줄에서 삽입; 기존 표·코드 내부 비활성 |
| E12 | 표 구조: 선택 범위 위/아래 행·좌/우 열 추가, 선택 행/열 삭제. 머리행 앞 삽입·머리행 삭제·마지막 본문 행/열 삭제 금지. 새 행은 정렬·너비 상속, 새 열은 왼쪽 정렬·자동 너비. 열 삭제 시 같은 위치 너비 제거, 최대 256열에서 추가 비활성 |
| E13 | 셀 범위 드래그·행/열/표 선택, 열 전체 왼쪽/가운데/오른쪽 정렬. 내용 지우기·셀 범위 Delete/Backspace는 구조·너비·정렬 보존. 명시적 표 삭제는 연결된 유효 너비 주석도 제거, 주변 문단 유지·빈 문서에 문단 제공 |
| E14 | 표 편집은 우클릭·Shift+F10 메뉴로 통합. 표 위 도구·전용 여백 제거, 표 바깥 세로 여백은 일반 문단과 같은 15px. 키보드 메뉴는 선택 셀 기준 배치. 범위 안 우클릭은 선택 유지, 밖은 해당 셀 지정. 메뉴 포커스 이동에도 대상 유지, 실행 전 문서·위치 검증. Esc 닫기·편집 초점 복원, 밝음/어두움·좁은 창·스크롤 경계 보정. 기존 48px 상단 서식 줄 유지 |
| E15 | Tab/Shift+Tab 셀 이동, 마지막 셀 Tab은 본문 행 추가 후 첫 셀 이동. 첫 셀 Shift+Tab은 제목 선택기로 이동. Enter/Ctrl+Enter 표 밖 문단. 구조·서식 명령은 조합 확정 후 공통 이력 1단계·타이핑과 분리. 메뉴/선택/취소는 dirty·revision 불변. 표 구조 메뉴는 렌더링 전용 |
| E16 | 렌더링 표 붙여넣기: Excel HTML·탭 구분 텍스트(TSV), 인용 셀·탭·개행·따옴표·빈 셀 지원. 빈 문단은 첫 복사 행을 머리행으로 신규 표 생성, 1행 복사는 빈 본문 행 보충. 기존 표는 현재 셀/선택 좌상단부터 덮어쓰기·필요 행/열 자동 확장, 범위 밖 내용·기존 열 너비/정렬 유지. 단일 값의 셀 범위 붙여넣기는 전체 선택 채움. 최대 256열·50,000셀·클립보드 문자열 10MB 제한 |
| E17 | 셀 범위 Ctrl+C/X·우클릭 복사는 머리행·본문·서식·열 너비/정렬을 HTML, 셀 값은 Excel 호환 TSV로 클립보드에 기록. 우클릭 복사는 드래그 범위/단일 셀 지원, 선택·text/revision/dirty/Undo 불변; 실패 안내, Windows 네이티브 클립보드 연동. 잘라내기는 구조 유지·선택 내용만 지움. 붙여넣기/잘라내기 각 공통 Undo 1단계. Excel 병합은 좌상단 값·나머지 빈 셀로 평탄화, 수식은 표시 값 반영. 일반 문단·코드 붙여넣기 유지 |
| E18 | 서식 줄 이미지 아이콘·네이티브 메뉴는 이미지 파일 선택을 먼저 표시. 파일/스크린샷 붙여넣기 지원, 원문/렌더링 요청 시 커서 보존·다중 이미지 Undo 1단계. PNG/JPEG/GIF/WebP/BMP, 최대 16개·합계 20MB, 호스트에서 파일 시그니처 검증. 취소·실패·문서 변경 후 늦은 응답은 본문 불변 |
| E19 | 코드 버튼·Ctrl+Alt+C는 양쪽 모드 코드 적용/해제. 선택에 코드 블록이 있으면 해당 블록을 본문으로 해제, 나머지 블록 유지. 내용·줄바꿈·컨테이너 유지, Markdown 특수문자는 리터럴 보존. 활성 상태/해제 툴팁 표시, 공통 Undo/Redo 지원 |
| E20 | 글머리 목록 옆 체크리스트 아이콘: 렌더링 현재/선택 문단·목록, 원문 현재/선택 줄을 작업 목록으로 전환. 전체가 체크리스트이면 체크 해제·일반 목록 유지, 혼합이면 기존 완료 상태 유지·나머지 미완료 생성. 인라인 서식·번호·중첩 보존, 표·제목·코드·보존 블록 포함 선택 비활성. 활성 상태·툴팁, 변환별 공통 Undo/Redo. 렌더링 체크박스 클릭 완료 전환, 내용 있는 항목 Enter는 미완료 다음 항목 생성 |
| E21 | Ctrl+F·찾기 버튼·호스트 find는 현재 모드 유지. 렌더링 서식 줄 아래 우측 검색창: 100ms 입력 지연·대소문자 무관 리터럴 검색·현재/전체·0/0·이전/다음·닫기. 제목/본문/표/목록/일반 코드, 인라인 서식 경계 연결·블록/원자 경계 분리. Mermaid 원문·이미지 내부 글자·보존 원문 노드 제외. 전체/현재 일치 색 구분·밝음/어두움·좁은 창 지원. Enter/F3·Shift+Enter/Shift+F3·화살표 버튼 순환 이동·자동 스크롤, IME 조합 키 제외. Esc 닫기·편집 초점 복원, 재열기 검색어 선택. 문서 변경 시 재검색·현재 위치 보정, 문서 로드 시 초기화·모드 전환 시 닫기. 강조는 ProseMirror decoration으로 text/revision/dirty/Undo·편집 선택 불변. 원문 CodeMirror 검색 유지 |
| M01 | 언어 `mermaid` 코드 블록을 대소문자 무관 인식. flowchart/graph·sequenceDiagram·classDiagram·stateDiagram-v2·erDiagram을 SVG로 표시. 기타 형식·이미지/아이콘 노드는 원문과 안내 표시 |
| M02 | Mermaid 12.0.0·DOMPurify 3.4.15 동봉, Mermaid 지연 로딩·DOMPurify 클립보드/SVG 공용. Folio base 테마·Dagre·한글 폰트·둥근 노드·회색 연결선·밝음/어두움 제공. SVG ShadowRoot로 본문 스타일 격리 |
| M03 | 블록별 원문 수정 버튼은 최신 Markdown AST·ProseMirror 코드 블록 순서·내용 검증으로 해당 본문에 이동. 중복·인용·목록 내 블록 지원. 위치 검증 실패 시 원문 안내 |
| M04 | 확대 모달: 25–300%·100%·폭 맞춤·스크롤·Esc 닫기·초점 복원. 폭 맞춤은 큰 그림의 전체 폭을 위해 25% 미만 축소 가능. 문서·테마·모드 변경 시 닫기 |
| M05 | SVG·배율·렌더 상태는 문서 밖에서 관리. 보기·테마 변경은 text/revision/dirty/Undo 불변. 실제 편집은 기존 이력·저장·복구 경로 사용. 무편집 원문 유지; 시각 편집 후 코드 내용·언어 유지, fence·공백은 serializer 정규화 가능. 추가 fence metadata 보존 제외 |
| M06 | 대기·렌더링·완료·오류·미지원·제한 상태 분리. 오류는 해당 블록 원문·요약·파서가 제공한 줄 위치 표시. 이전 그림을 최신 결과로 표시하지 않음. 문서 나머지 편집·저장 유지 |
| M07 | 20,000자 상한·Mermaid maxEdges 200·단일 큐·가시 영역 ±400px 우선 렌더. 문서 epoch·노드 generation으로 오래된 결과 폐기. LRU 24개/8MB·문서 전환 초기화·표시마다 SVG ID/참조 재작성 |
| M08 | securityLevel strict·HTML 라벨 비활성화·전체 앱 설정 보호. 문서 frontmatter/init 설정은 앱 정책을 덮어쓰지 못함. 일반 classDef/style 색상 허용, 외부 리소스 스타일 거부. DOMPurify·SVG/CSS 정제·기존 CSP 유지, 링크/callback 실행 없음 |
| F01 | UTF-8·BOM 선택 유지, 10MB 상한, 비UTF-8·NUL 포함 파일 거부. 수정 파일은 기존 주요 개행 방식 유지 |
| F02 | 저장: snapshot 요청 → 디스크 SHA-256 비교 → 임시 파일 flush → 교체 직전 재검사 → File.Replace/Move → 저장 내용 ACK |
| F03 | 이전 revision 저장 완료는 이후 변경을 지우지 않음. 문서 전환·종료 전에 저장 후 최신 dirty 상태 재확인 |
| F04 | 외부 변경 4초 확인. 저장 충돌은 덮어쓰기·다른 이름 저장·취소. OS·다른 앱과의 완전한 분산 잠금은 제공하지 않음 |
| F05 | 변경 문서 복구본 3초 저장. 복구 기록 완료를 기다린 후 문서 전환·저장·종료 시 정리 |
| F06 | 이미지 삽입/붙여넣기는 문서 폴더 assets에 고유 이름 복사, 원본 파일 유지. 미저장 문서는 프로필 drafts/<식별자>/assets에 보관·즉시 표시, 복구본에 식별자 유지. 첫 저장은 Undo/Redo 대상 포함 임시 이미지 복사; 다른 폴더 저장은 참조 이미지 복사. 기존 이미지 덮어쓰기 금지 |
| W01 | C# WinUI 3 네이티브 창·TreeView + WebView2. 웹 자산 앱 동봉, 로컬 문서 편집 오프라인 |
| W02 | 호스트 통신은 앱 origin만 허용. 명령·URL 스킴 검증. 외부 내비게이션·권한 요청 차단. 문서 이미지 경로는 현재 문서 폴더 또는 해당 미저장 보관 폴더 내부만 허용 |
| W03 | 밝은/어두운 테마·목차 너비·창 크기·최근 문서 최대 20개 저장. 전체 경로 정규화·대소문자 무관 중복 제거·최근 열기/저장 순서. 기존 8개 기록 유지, 누락/잘못된 항목 보정. 단일 앱 인스턴스 |
| W04 | Configuration: 본문·표 줄 높이 1.00–3.00배(기본 1.95), 목차 줄 높이 1.00–3.00배(기본 2.00), 문서 왼쪽/가운데 배치(기본 가운데). 0.05배 증감·직접 입력 |
| W05 | 변경 즉시 미리보기. 저장 시 settings.json 임시 기록 후 교체, 재실행 복원. 취소 시 이전 값 복원. 기본값 버튼은 저장 전 미리보기. 이전 설정 파일은 누락 항목 기본값 적용·범위 보정 |
| W06 | 목차 실제 행 높이 = 12px × 배수 + 8px. 왼쪽 배치는 880px 최대 폭 유지·좌우 안쪽 여백 28px. 본문 문단 여백·표 열 정렬 유지. 설정은 문서 내용·dirty·공통 편집 이력과 독립 |
| W07 | 파일명·미저장 표시를 높이 56 DIP 앱 바에 통합, 긴 이름 생략·전체 경로 툴팁. 웹 제목 줄 제거, 서식·모드 전환 48px 한 줄. 편집 영역 높이 기존 대비 96 DIP 증가, 본문 위 안쪽 여백 28px |
| W08 | 단일 인스턴스 파일 전달: 같은 사용자 named pipe, UTF-8 경로 32KB·대기 32건 제한, 전달 ACK. 시작·복구·다이얼로그 처리 완료 후 열기. 한글·공백 경로, 최소화 복원, 같은 문서 재열기 방지, 미저장 저장·버리기·취소 유지 |
| W09 | 상단 최근 문서·Ctrl+Shift+O: 검색 가능한 네이티브 목록. 파일명 강조·전체 경로 줄바꿈, 경로 생략 없음. 최대 20개 스크롤·파일명/폴더 검색·항목 수·빈 상태. 선택 후 열기/Enter·더블클릭. 취소는 본문 유지; 선택 열기는 기존 미저장 확인·파일 오류 처리 경로 사용. 없는 파일도 기록 유지 |
| W10 | 앱 바 파일명 클릭: 읽기 전용 전체 경로·자동 전체 선택·Ctrl+C·복사 버튼. 우클릭: 전체 경로 복사. Windows 텍스트 클립보드로 Notepad 등 붙여넣기 가능. 미저장 신규 문서는 안내·복사 비활성, 문서 내용/경로/dirty 불변 |
| P01 | Windows x64 self-contained 폴더·ZIP·단일 Inno Setup EXE. 한국어/영어 UI·사용자별 설치·시작 메뉴·선택 바탕 화면·Windows 앱 제거 등록 |
| P02 | 경량 기본: WebView2 Bootstrapper 내장, 미설치 시 다운로드. 오프라인 옵션: x64 Evergreen Standalone 내장. HKLM/HKCU 확인 → 미설치 시 /silent /install → 설치 여부 재확인 → 앱 설치. 실패 시 재시도 안내. 기존 runtime·제거 후 공유 runtime 유지 |
| P03 | 동일 AppId 재설치·업데이트. 설치 대상 실행 중이면 업데이트·제거 거부, 강제 종료 없음. 제거는 기록된 앱 파일만 삭제; 사용자 문서·설정·복구본 유지. 구형 제거 스크립트 폐기 |
| P04 | 빌드 도구·runtime Authenticode 발행자 검증, 설치 EXE SHA-256·빌드 메타데이터 생성. 기존 설치파일은 보관 후 교체. 앱·설치파일 코드 서명 미적용 |
| P05 | 선택 파일 연결(기본 체크): HKCU ProgID·OpenWith·Default Apps·우클릭 명령으로 .md/.markdown 등록, 실행·문서 경로 따옴표 처리, Folio 아이콘. 완료 화면에서 Windows 기본 앱 설정 열기; 기존 기본값·UserChoice 강제 변경 없음. 제거 시 Folio 소유 등록만 정리 |

범위 밖: Store/MSIX 서명 배포, Mermaid 노드 드래그 편집·블록 내 분할 편집·SVG/PNG 내보내기·ELK 설정, 수식·MDX 전용 편집, 섹션 드래그 이동, 다중 탭·협업, Mermaid 원문 이동 외 문자 단위 모드 간 선택 매핑, 임의 Markdown 바이트 단위 왕복 보존, ARM64 전용 빌드.
